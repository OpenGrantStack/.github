{
  "name": "open-grant-stack",
  "version": "1.1.0",
  "description": "Structured grant eligibility data for OpenGrantStack",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.js",
    "validate:languages": "node scripts/validate-languages.js",
    "validate:all": "npm run validate && npm run validate:languages",
    "merge": "node scripts/merge-grants.js",
    "build": "npm run validate:all && npm run merge"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  },
  "license": "MIT"
}